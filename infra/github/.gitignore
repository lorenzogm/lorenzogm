# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.enc
tfplan
*.tfplan

# Environment (private keys — encrypted .env is committed)
.env.keys
.env.example
.env.backup

# Local overrides
.env.local
*.tfvars
